import PagePlaceholder from "@/components/PagePlaceholder";

export default function Orders() {
  return (
    <PagePlaceholder
      title="Orders"
      description="Create and track orders through the Pending → Processing → Completed / Cancelled lifecycle."
    />
  );
}
